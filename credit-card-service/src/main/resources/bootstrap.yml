spring:
  application:
    name: cardservice
  output:
    ansi:
      enabled: always
  cloud:
    config:
      profile: dev        #profile, config server {profile}
      label: master       #git, config server {label}
      discovery:
        enabled: true
        serviceId: config-server